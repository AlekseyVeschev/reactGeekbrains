
const profileInit = {
   name: "Name",
}

const profileReducer = (state = profileInit, action) => {
   switch (action.type) {

      default:
         return state
   }
}
export default profileReducer